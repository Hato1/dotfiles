# Dot files Linux Mint

1. Install Typora to read this document.

2. Install Oh-My-ZSH 

   1. `sudo apt install zsh`

   2. `sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)`

   3. ```bash
      chsh -s $(which zsh)
      ```

   4. Logout, and log back in.

3. Install Poetry (Special step for Ubuntu, scroll down)

4. Install PyCharm

   1. Add CodeGlance Pro

   2. Add Sourcery + confirm Key

5. 

6. Install Git, GitKraken, Sublime Text